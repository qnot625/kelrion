# Browser-facing API prefix. Keep /api when the frontend and API share a domain
# or when using the Vite development proxy.
VITE_API_BASE_URL=/api

# Development proxy target used by vite.config.ts.
KLERION_API_ORIGIN=http://localhost:3001
