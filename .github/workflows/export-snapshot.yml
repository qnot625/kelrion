name: Export Developer 5 Snapshot

on:
  push:
    branches:
      - export/developer-5
  pull_request:
    branches:
      - Developer-5

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Create source archive
        run: tar --exclude=.git -czf /tmp/developer-5.tar.gz .
      - uses: actions/upload-artifact@v4
        with:
          name: developer-5-snapshot
          path: /tmp/developer-5.tar.gz
          if-no-files-found: error
          retention-days: 1
