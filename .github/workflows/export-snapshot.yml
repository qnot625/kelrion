name: Export Developer 3 Snapshot

on:
  push:
    branches:
      - export/developer-3
  pull_request:
    branches:
      - Developer-3

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Create source archive
        run: tar --exclude=.git -czf /tmp/developer-3.tar.gz .
      - uses: actions/upload-artifact@v4
        with:
          name: developer-3-snapshot
          path: /tmp/developer-3.tar.gz
          if-no-files-found: error
          retention-days: 1
